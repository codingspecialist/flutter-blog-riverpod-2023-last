import 'package:flutter/material.dart';

class PostWriteForm extends StatelessWidget {
  const PostWriteForm({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
